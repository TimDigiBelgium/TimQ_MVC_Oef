@model TimQ_MVC_Oef.Models.Categorie


@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Categorie</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Naam)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Naam)
        </dd>
    
        <p>
            @foreach (var item in ViewData["ComponentsList"] as IEnumerable<TimQ_MVC_Oef.Models.Component>)
            {
               @item.Naam
            }
        </p>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CategorieID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
